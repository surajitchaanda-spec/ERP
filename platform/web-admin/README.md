# ERP Web Admin

React + Vite admin console for managing ERP master data, communications, and reporting dashboards.

## Development

```bash
npm install
npm run dev
```

The dev server proxies API requests to `http://localhost:3000`.
