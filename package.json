{
  "name": "erp-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "platform/backend",
    "platform/web-admin"
  ],
  "scripts": {
    "lint": "npm run lint --workspaces",
    "test": "npm run test --workspaces"
  }
}
